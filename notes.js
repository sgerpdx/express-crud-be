//// app.js
// app.use(express.static('path from root, not lib'));
// the root is where server.js or from where you type node server.js in the terminal (usually the same)
// app.use(express.static(`${__dirname}/../public`));   ...(this starts path in app, not root)

// then go to localhost:7890/ to test (w/ h1 in index.html so that there's something to display)

//
